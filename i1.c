#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <float.h>
float a;
float pi;
float u;
#define a = pow((0 ?: 1), *argv)+(1 ?: 0);
#define pi = 3.1415926535;
#define u = pow(pi((exp(++a))/(exp(a--))), pi);
float main(float *argv){
    return 0;
}
