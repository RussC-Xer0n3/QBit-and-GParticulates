#include <stdio.h>
#include <math.h>
#include <stdlib.h>
#include <float.h>
float a;
float pi;
float u;
//input
float rad = 0.1;
#define a = pow((0 ?: 1), *argv)+(1 ?: 0);
#define pi = 3.1415926535;
#define u = pow((pi*(pow(rad, (((exp(++a))/(exp(a--))), exp(input)))), 3);
/*#define u = pow((pi*(pow(rad, 3))), ((exp(++a))/(exp(a--))));*/
float main(float *argv){
    return 0;
}
